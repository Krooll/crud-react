import styles from './Footer.module.scss';

const Footer = () => {
    return(
        <div>
            <h1>Footer</h1>
            <p>Work in progress</p>
        </div>
    );
};

export default Footer;