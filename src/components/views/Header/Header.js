import styles from './Header.module.scss';

const Header = () => {
    return(
        <div>
            <h1>Header</h1>
            <p>Work in progress</p>
        </div>
    );
};

export default Header;