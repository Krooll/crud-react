import styles from './HomePage.module.scss';

const HomePage = () => {
    return(
        <div>
            <h1>HomePage</h1>
            <p>Work in progress</p>
        </div>
    );
};

export default HomePage;